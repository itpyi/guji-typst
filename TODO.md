# Typst 双行夹注排版

此文檔用於指導agent完成工作。

## 基本格式

請 agent 在 `style.typ` 中寫一模板，並在 `main.typ` 中用 show rule 調用。先寫一個空模板即可，將字體設定爲 KingHwa_OldSong.


將紙張改成橫向，在紙張上畫出一網格，模擬古籍中的格子，只畫豎線不畫橫線。此後豎着的一排稱爲一行。給模板一參數 `char-per-col` 表示一行放置的正文字數，預設值爲 16。然後忽略 main 中的所有標點，將其中文字從上到下、從右到左填進格子裏，每個格子一個漢字。字號請調整爲放在格子中居中的形狀。

在 main 中標題後和分段處另起一行。

實現雙行夾註。{}中內容實際上是雙行夾註，如果你認爲這種方式不好處理，也可以定義一個新的函數 `zhu` 將內容放在裏面。夾註的字數不計標點設爲 $N$，則它應佔據 $N/4$ 向上取整個格子。計算好夾註佔據的格子數後，將其文字填入格子，填法爲：一個正文行內的格子，如今夾註有兩行，先填滿第一行，再填第二行；如果夾註佔據的格子跨越了不同正文行，則將不同正文行以此按照上述邏輯填滿雙行。

非常好，整體字號可以再大一點，把格子撐滿一點。線換成紅色。

現在似乎只有單頁，如果內容超過一頁則後續不顯示。請你處理這個問題。

【已完成】已修復分頁邏輯中的語法/佈局錯誤，內容可正常跨頁顯示。


請你在一級標題前分頁（第一個一級標題前自然不分頁）。並在二級標題前空行。

【已完成】已實現：非首個一級標題前自動分頁；二級標題前預留一行。


將夾註邏輯微調：夾註中有 4k+1 個字時，第一小列放2k+1個，第二小列放2k個。其他情況保持不變。

【已完成】已實現：當夾註字數為 4k+1 時，第一小列 2k+1、第二小列 2k；其餘情況沿用原規則。


此前的排版忽略了所有標點符號。現在如下放回標點：完全不佔格子，而將標點擠壓放置與上一個字格子的右下角，並使用紅色。

【已完成】已實現：標點不佔格，壓置於上一字格右下角並設為紅色（正文與夾註均生效）。


三級標題前也空一行

【已完成】已實現：三級標題前同樣預留一行。

夾註字數爲4k+2時，改成 (2k+1)*2 而非 (2k+2)+2k，其餘邏輯不變。

【已完成】已實現：夾註字數為 4k+2 時按 (2k+1, 2k+1) 分配；其餘規則不變。


格子上下加兩個粗橫線，紅色，左右兩端細豎線稍外再加兩個粗細線。即用一封閉的粗矩形框住整個區域。

【已完成】已實現：格區外側新增紅色封閉粗矩形框（上下粗橫線 + 左右外側粗豎線）。

每一頁改成奇數列，並且最中間一列設置成空列。

【已完成】已實現：每頁使用奇數列，且中間列保留為空列（不排正文與夾註）。

每一頁最中間的空列倒數第5格以夾註形式放中文頁碼（只放數字）。

【已完成】已實現：每頁中間空列倒數第 5 格以夾註樣式放中文頁碼（僅數字）。
每一頁最中間的空列正數第四格起以正文字號放此處內容對應的一級標題。

【已完成】已實現：每頁中間空列自正數第 4 格起，以正文字號縱排該頁對應的一級標題。


重構代碼：將上面兩個放在最中間一行的內容整理到版心模塊裏。可以用一個版心函數在模板中實現，而版心函數另外定義在模板中。

【已完成】已重構：中間空列的一級標題與頁碼渲染已抽離為獨立版心函數 `render-banxin(...)`，模板中統一調用。

繼續美化版心：在上面標題的上方和下面頁碼的下方繪製魚尾，即如“【”形狀的部件，上方的開口向下，下方的開口向上。最上橫線到上方魚尾用一粗線連接，即中縫。

【已完成】已實現：版心新增上下魚尾（上開口向下、下開口向上），並以粗線中縫連接最上橫線與上魚尾。

三級標題前空兩格

【已完成】已實現：三級標題前空兩格（在新列起始處下移兩格再排題）。


做一個目錄，放於文件開頭，格式同後續正文格式，一級標題爲目錄。目錄包括到三級標題，一級標題頂格，二級縮進一個，三級縮進兩格。頁碼放在相應行最後一個，採取夾註格式。

【已完成】已實現：文首新增目錄（一級標題為“目錄”），含一至三級條目；一級頂格、二級縮進一格、三級縮進兩格；頁碼置於行末並以夾註格式顯示。

目錄中忽略標題裏的夾註。

【已完成】已實現：目錄條目會忽略標題中的夾註內容（僅保留標題正文）。

版心中標題部分改爲去掉夾註的二級標題。（此前爲一級標題。二級標題去掉夾註的方法已在目錄中實現。）如沒有對應的二級標題，則使用去掉夾註的一級標題。

【已完成】已實現：版心標題優先顯示去夾註的二級標題；若無對應二級標題，則回退為去夾註的一級標題。


請你給模板加一個變量，表示是否顯示標點，在調用時用with來調節。

【已完成】已實現：`template` 新增具名參數 `show-punct`（預設 `true`），可在調用時以 `#show: template.with(show-punct: false)` 關閉標點顯示；參數已向下傳遞至 `layout-all-blocks` 及 `layout-block-segments`，正文與夾註的標點渲染均受其控制。

【以上均已完成，從此處開始工作】